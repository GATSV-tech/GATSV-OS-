# DB modules:
#   client.py     — Supabase client singleton (Slice 1)
#   migrations/   — SQL migration files (Slice 2)
#   events.py     — Event CRUD (Slice 2)
#   entities.py   — Entity CRUD + memory upsert (Slice 2)
#   actions.py    — Action CRUD (Slice 2)
#   approvals.py  — Approval queue CRUD (Slice 2)
